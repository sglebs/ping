# Deployment descriptor in case you want to use https://github.com/sglebs/ndeploy
apps:
  - name: "ping-{{ area }}-{{ scenario|lower }}"
    git : "git@github.com:sglebs/ping.git"
    branch : "locust"
